/bin/
*.test
/coverage.out
.DS_Store

# Rendered .tmpl outputs (BuildActions writes to the stripped-suffix sibling).
# Add one line per new .tmpl file.
modules/git/files/.gitconfig
modules/neovim/files/nvim/lazy-lock.json

# Internal device-keyed symlinks (set by modules/hyprland/install.sh).
modules/hyprland/files/hypr/monitors.conf
