[Unit]
Description=Hyprlock watchdog - auto-restart on crash
After=graphical-session.target

[Service]
Type=simple
ExecStart=hyprlock
Restart=on-failure
RestartSec=2
ExecStartPre=hyprctl --instance 0 'keyword misc:allow_session_lock_restore 1'

[Install]
WantedBy=graphical-session.target
