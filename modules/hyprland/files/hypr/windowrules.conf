##################
### WORKSPACES ###
##################

# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

workspace = special:scratchpad, on-created-empty:kitty, gapsout:40, gapsin:20
workspace = special:music, on-created-empty:spotify, gapsout:20, gapsin:10
workspace = special:files, on-created-empty:dolphin, gapsout:20, gapsin:10
workspace = special:monitoring, on-created-empty:[float] kitty -e btop, gapsout:30, gapsin:15
workspace = special:obsidian, on-created-empty:obsidian, gapsout:25, gapsin:12

##############
### LAYERS ###
##############

layerrule = blur on, match:namespace swaync-notification-window
layerrule = blur on, match:namespace swaync-control-center
layerrule = ignore_alpha 0.3, match:namespace swaync-notification-window
layerrule = ignore_alpha 0.3, match:namespace swaync-control-center

# Blur behind rofi
layerrule = blur on, match:namespace rofi
layerrule = ignore_alpha 0.3, match:namespace rofi

###############
### WINDOWS ###
###############

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more

windowrule {
    # Ignore maximize requests from all apps.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

windowrule {
    name = world-of-warcraft
    match:title = World of Warcraft
    idle_inhibit = always
    opacity = 1
    no_anim = on
    no_blur = on
    no_dim = on
    no_shadow = on
    render_unfocused = on # This fixes game freezing when moving workspaces
    immediate = on
    fullscreen = on
    fullscreen_state = 2 2 # internal=2 (fullscreen), client=2 (fullscreen) - correct 0.54 syntax
}

windowrule {
    name = Games
    match:title = Zoo Tycoon
    idle_inhibit = always
    opacity = 1
    no_anim = on
    no_blur = on
    no_dim = on
    no_shadow = on
    fullscreen = on
    fullscreen_state = 3
    render_unfocused = on # This fixes game freezing when moving workspaces
    monitor = DP-1 # HYTE case monitor — not detected since 2026-07 reinstall; fix name when reconnected
}

windowrule {
    name = tradeskillmaster-float
    match:title = TradeSkillMaster Application.*
    float = yes
    monitor = DP-5 # Samsung (bottom primary); port renumbered on 2026-07 reinstall
    workspace = 4
    move = 2831 48
    size = 600 600
}

windowrule {
    name = TSMApplication
    match:title = TSMApplication 
    float = yes
    monitor = DP-5 # Samsung (bottom primary); port renumbered on 2026-07 reinstall
    workspace = 4
    move = 2831 48
    # size = 600 600
}

windowrule {
    name = battlenet-float
    match:title = Battle.net
    float = yes
}

windowrule {
    name = devtools-windowed
    match:class = firefox
    match:initial_title = ^$
    float = yes
    size = 1570 970
}

windowrule {
    # Playwright / MCP automation launches headed Chrome; keep it a
    # predictable floating window instead of tiling into the workspace.
    name = playwright-windowed
    match:class = google-chrome
    float = yes
    size = 1920 1080
    center = yes
}

